# ===========================================
# FitTrack App - 本番用環境変数テンプレート
# ===========================================
# 設定手順:
# 1. このファイルを .env にコピー
# 2. 本番用JWT秘密鍵を生成:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 3. 各環境に応じた値に変更
# 4. 本番環境では .env ファイルを秘匿情報として管理
# ===========================================


# 基本設定
NODE_ENV=development
PORT=8000

# データベース設定（PostgreSQL）
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_database_password
DB_NAME=fittrack_db
DB_PORT=5432

# JWT認証設定
# ⚠️  重要: 本番環境では必ず新しい64文字秘密鍵を生成
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET_KEY=your_jwt_secret_minimum_64_characters_generated_with_crypto_randomBytes
JWT_EXPIRES_IN=24h

# CORS設定
# 本番環境用フロントエンドURL
CORS_ORIGIN_PROD=https://your-production-domain.com
# 開発環境用フロントエンドURL
CORS_ORIGIN_DEV=http://localhost:5173

# セキュリティ設定
BCRYPT_ROUNDS=12

STRAVA_CLIENT_ID=あなたのクライアントID
STRAVA_CLIENT_SECRET=あなたのクライアントシークレット
STRAVA_REDIRECT_URI=http://localhost:8000/api/strava/callback
ENCRYPTION_KEY=32文字のランダムな文字列を生成

